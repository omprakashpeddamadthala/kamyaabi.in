spring:

  datasource:
    url: jdbc:h2:mem:kamyaabidb
    driver-class-name: org.h2.Driver
    username: sa
    password:

  h2:
    console:
      enabled: true
      path: /h2-console

  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    show-sql: true
    hibernate:
      ddl-auto: create

  security:
    oauth2:
      client:
        registration:
          google:
            client-id: ${GOOGLE_CLIENT_ID:}
            client-secret: ${GOOGLE_CLIENT_SECRET:}
            scope: openid,profile,email
            redirect-uri: "{baseUrl}/api/auth/success"
        provider:
          google:
            authorization-uri: https://accounts.google.com/o/oauth2/v2/auth
            token-uri: https://www.googleapis.com/oauth2/v4/token
            user-info-uri: https://www.googleapis.com/oauth2/v2/userinfo
            user-name-attribute: email

  mail:
    host: smtp.gmail.com
    port: 587
    username: omprakashornold@gmail.com
    password: vmnk iqno jjwt fqfy
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

management:
  health:
    mail:
      enabled: false

  security:
    oauth2:
      client:
        registration:
          google:
            client-id: ${GOOGLE_CLIENT_ID:}
            client-secret: ${GOOGLE_CLIENT_SECRET:}
            scope: openid,profile,email

app:
  jwt:
    secret: mySuperSecretKeyForHS512AlgorithmThatIsAtLeast512BitsLongAndSecureEnoughForJWTSigningAndVerificationPurposes
    expiration-ms: 86400000

  cors:
    allowed-origins: http://localhost:3000

  razorpay:
    key-id: rzp_test_SWwEXONa1b80MB
    key-secret: oAxmWoG4D9Ocn3k9G1tG879s
  email:
    enabled: true
    from-email: noreply@kamyaabi.in
    from-name: Kamyaabi
    admin-emails:
      - admin@kamyaabi.in
    smtp:
      host: smtp.gmail.com
      port: 587
      username: omprakshornold@gmail.com
      password: anho wcln smjj hbbf
      starttls-enabled: true
      auth: true
